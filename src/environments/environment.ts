// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyDQoNiZTViRFZRYH5TkUN1ctLsBcc_cyFQ',
    authDomain: 'testing-system-badb2.firebaseapp.com',
    databaseURL: 'https://testing-system-badb2.firebaseio.com',
    projectId: 'testing-system-badb2',
    storageBucket: 'testing-system-badb2.appspot.com',
    messagingSenderId: '812614898635'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
